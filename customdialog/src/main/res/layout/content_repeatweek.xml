<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/StyleContentHeader"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/custom_repeatweek">

    <include layout="@layout/content_repeatweekbody"/>

    <LinearLayout
        android:id="@+id/llLowerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:gravity="right"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvContentDecline"
            style="@style/StyleDecline"
            android:textColor="@color/colorLightGrey"/>

        <TextView
            android:id="@+id/tvContentAccept"
            style="@style/StyleAccept"
            android:textColor="@color/colorBrightRed"/>
    </LinearLayout>
</FrameLayout>